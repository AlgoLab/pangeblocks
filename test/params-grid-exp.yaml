PATH_MSAS: "/data/pangeblocks-experiments/msas"
PATH_OUTPUT: "/data/pangeblocks-experiments/output-small-msas"
OPTIMIZATION:
  OBJECTIVE_FUNCTION:
    # - "nodes"
    - "strings"
    # - "weighted"
  PENALIZATION: # used only with "weighted"
    - 3 
  MIN_LEN: # used only with "weighted"
    - 3  
  TIME_LIMIT: 30 # time limit to run the ILP (minutes)
LOG_LEVEL: "INFO"