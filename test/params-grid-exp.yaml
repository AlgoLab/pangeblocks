PATH_MSAS: "test"
PATH_OUTPUT: "/data/pangeblocks-experiments/output-test"
OPTIMIZATION:
  OBJECTIVE_FUNCTION:
    - "nodes"
    - "strings"
    - "weighted"
  PENALIZATION: # used only with "weighted"
    - 7 
  MIN_LEN: # used only with "weighted"
    - 10  
  TIME_LIMIT: 30 # time limit to run the ILP (minutes)
LOG_LEVEL: "INFO"