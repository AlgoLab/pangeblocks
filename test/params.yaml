PATH_MSAS: "/data/alessia/covid/msa"
PATH_OUTPUT: "output-covid"
THRESHOLD_VERTICAL_BLOCKS: 3
OPTIMIZATION:
  OBJECTIVE_FUNCTION: "nodes" # nodes, strings, weighted, depth
  PENALIZATION: 3 # used only with "weighted"
  MIN_LEN: 2 # used only with "weighted"
  MIN_COVERAGE: 0.2 # used only with "depth"
  TIME_LIMIT: 10 # time limit to run the ILP (minutes)
LOG_LEVEL: "INFO"
THREADS: 16