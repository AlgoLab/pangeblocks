"""
a callback receives as input all the variables the monitor collect
and can perform any operation with those values
"""
# write info to a csv
class Callback: 

    def __call__(self, values):
        pass 
    
# write info to a json

# write 